import styled from 'styled-components'
import { cardBasicStyle } from '../../../theme/globalStyle'

export const OneFeatureWrapper = styled.article`
  ${cardBasicStyle}

  display: grid;
  justify-items: center;
  gap: 4rem;
  padding: 5rem 3rem;
`
export const ImageStyles = styled.img`
  height: 7rem;
`
